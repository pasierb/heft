# heft

Seamless worktrees management for `herdr` users

## Config

heft comes with sane defaults, but if you want to change something here options:

`worktrees_dir` - heft is using `<project root>/.worktrees` as a place where it stores all the worktrees
